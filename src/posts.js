/**
 * Post registry — generated from src/posts/*.md by
 * scripts/generate-posts.js (ordered by frontmatter date, newest first).
 * Add a file with a `title`/`date` frontmatter, then run `npm run generate`.
 */
export { posts, getPost, featuredPost } from './posts/generated.js'
